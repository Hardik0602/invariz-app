import { Text, View } from 'react-native'

const settings = () => {
  return (
    <View className='items-center justify-center flex-1'>
      <Text className='text-xl font-extrabold'>settings</Text>
    </View>
  )
}

export default settings